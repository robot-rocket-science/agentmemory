---
description: Show all locked beliefs (non-negotiable constraints).
allowed-tools: Bash
---

Run the agentmemory locked CLI command:

```bash
uv run --project /Users/thelorax/projects/agentmemory agentmemory locked
```

Display the output to the user. Do not add commentary.
