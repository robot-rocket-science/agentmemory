---
description: Scan a project directory and ingest it into agentmemory.
argument-hint: Path to project directory (e.g. . or ~/projects/myapp)
allowed-tools: Bash
---

Run the agentmemory onboard CLI command with the provided path argument:

```bash
uv run --project /Users/thelorax/projects/agentmemory agentmemory onboard <path>
```

Display the output to the user. Do not add commentary.
