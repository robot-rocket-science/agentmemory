---
description: Search agentmemory beliefs for a query.
argument-hint: Search query (e.g. "retrieval architecture" or "user preferences")
allowed-tools: Bash
---

Run the agentmemory search CLI command with the provided query:

```bash
uv run --project /Users/thelorax/projects/agentmemory agentmemory search <query>
```

Display the output to the user. Do not add commentary.
