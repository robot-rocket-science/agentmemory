[project]
name = "agentmemory"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "datasketch>=1.9.0",
    "numpy>=2.4.4",
    "scipy>=1.17.1",
]

[dependency-groups]
dev = [
    "pyright>=1.1.408",
]

[tool.pyright]
pythonVersion = "3.12"
typeCheckingMode = "strict"
